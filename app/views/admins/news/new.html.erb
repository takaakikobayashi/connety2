<div class="row">
  <div class="col-xs-8 col-xs-offset-4">

    <div class="sign_up">
      <h4><mark>お知らせ新規追加</mark></h4>
    </div>      

    <%= form_for @news, url: admins_news_create_path do |f| %>
      <%= render "layouts/error_message", title: @news %>
      
      <table border="0">
      	<tr style="height:70px">
          <th>カテゴリ名</th>
          <td>
            <% f.label :category %>
            <%= f.text_field :category, autofocus: true, autocomplete: "category", class: "form-control" %>
          </td>
        </tr>

        <tr style="height:70px">
          <th>お知らせ内容</th>
          <td>
            <% f.label :content %>
            <%= f.text_field :content, autofocus: true, autocomplete: "content", class: "form-control" %>
          </td>
        </tr>
      </table>

      <div class="actions">
        <%= f.submit "新規登録", class:"btn btn-success center-block btn-lg", style:"width:250px" %>
      </div>
      
    <% end %>
      
  </div>
</div>